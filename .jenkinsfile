pipeline {
agent any
stages {
    stage('test') {
        steps {
                sh 'npm install'
                sh 'sample-bs-local-test'
                sh 'sample-bs-test'
            }
        }
    }
}
